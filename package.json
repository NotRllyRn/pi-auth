{
  "name": "pi-auth",
  "version": "0.1.0",
  "description": "Named OAuth credential profiles for Pi",
  "type": "module",
  "keywords": ["pi-package"],
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.80.6",
    "@earendil-works/pi-coding-agent": "^0.80.6",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "files": ["src", "README.md", "docs/usage.md"],
  "license": "MIT"
}
